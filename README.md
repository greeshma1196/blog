# Greeshma Mathew — Personal Blog

A software engineer's blog exploring new concepts, experiments, and personal growth. Built with Jekyll and hosted on GitHub Pages.

## Setup

```bash
make install   # Install dependencies
make serve     # Run locally with live reload (http://localhost:4000)
make build     # Build for production
make clean     # Clean build output
```
